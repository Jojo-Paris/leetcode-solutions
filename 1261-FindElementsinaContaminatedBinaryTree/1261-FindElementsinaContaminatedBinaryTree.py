
    def __init__(self, root: Optional[TreeNode]):
        
        def recover(tempRoot, value):
        self.A = A = set()

            if not tempRoot: return None
            
        recover(root, 0)            
#         self.right = right
class FindElements:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
# Definition for a binary tree node.
# class TreeNode:


    def find(self, target: int) -> bool:
        return target in self.A


            A.add(value)
            recover(tempRoot.left, 2*value + 1)
            recover(tempRoot.right, 2*value + 2)

[
